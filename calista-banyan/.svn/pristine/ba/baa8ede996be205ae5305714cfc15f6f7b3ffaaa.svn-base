## How to build project

**Install external libraries(if necessary):**

```
mvn post-clean
```

**Build project:**

*Package project:*

```
mvn package -DskipTests=true
```
*Or package and install to local maven repository*

```
mvn install package -DskipTests=true
```

